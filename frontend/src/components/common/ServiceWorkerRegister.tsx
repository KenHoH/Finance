"use client";

/**
 * Service worker disabled during active development to prevent stale cache.
 * Re-enable after production build if PWA/offline support is needed.
 */
export function ServiceWorkerRegister(){
  return null;
}
